---
- name: Create FortiGate Firewall Address Object
  hosts: all
  connection: httpapi
  collections:
    - fortinet.fortios
  gather_facts: false

  tasks:
    - name: Create a new firewall address object for a test server
      fortinet.fortios.fortios_firewall_address:
      state: “present”
      firewall_address:
      name: test_123
      subnet: 10.1.1.1 255.255.255.0
