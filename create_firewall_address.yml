---
- name: Create FortiGate Firewall Address Object
  hosts: all
  connection: httpapi
  collections:
    - fortinet.fortios
  gather_facts: false

  tasks:
    - name: Create a new firewall address object for a test server
      fortios_firewall_address:
        state: "present"
        address:
          name: "Test-Server-01"
          subnet: "192.168.100.101/32"
          comment: "Managed by Ansible AWX"
